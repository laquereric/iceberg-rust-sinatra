# frozen_string_literal: true

RSpec.describe Iceberg::Rust::Sinatra do
  it "has a version number" do
    expect(Iceberg::Rust::Sinatra::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
